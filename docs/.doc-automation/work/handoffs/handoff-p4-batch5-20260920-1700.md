# 交接记录 — Batch-5（介绍页与操作页改写 + 站点托管）

> 时间：2026-09-20
> 阶段：P4 已通过（退出码 0），运行模式 generate（范围限定为两页）

## 已完成步骤

- P0 增量：`context-brief.md` 追加「第三轮」一节。用户三选一选定**改写现有两页**，
  受众选定**还没安装的人**；不新增页面（新增会与 index / quickstart 构成第二套真源）
- P1：`doc-plan.md` 追加 Batch-5，页数预算维持 6 页每语言，只改密度不加页；
  `--phase p1` 退出码 0
- P2-3：重写 `publish/index.md`、`publish/quickstart.md` 与两份 `en/` 对应页。
  index 首屏改为「现在 / 装上 mp 之后」对照图 + 能力表 + 真实手机截图（E010）；
  quickstart 首屏改为五步总览图，预览流程从 4 步收敛到 3 步，每步都带预期
- P4：首轮 4 个阻断项（`workflows/secrets-and-decisions.md` 中英两份的编号列表缺
  `起点：` 与末步 `- 预期：`），改为无序列表后复跑退出码 0
- 镜像：`publish/` 四页 → `docs/guide/`；`workflows/secrets-and-decisions.md` 的修正
  回灌 `publish/`，两边继续逐字节相同
- 站点：`docs/guide/_config.yml`、`docs/guide/_layouts/default.html`、
  `.github/workflows/pages.yml`；`publish-post.mjs --llms` 产出的 `llms.txt`
  与 `llms-full.txt` 已镜像进 `docs/guide/`

## 未做的事

- **未提交、未推送、未开启 Pages**：这三步都是对外动作，等用户确认
- 未改 `README.md` 的手册入口：站点地址还没上线，现在写进去就是一条 404 链接。
  上线并核对通过后再补
- 未设仓库 About（description / homepage / topics）：三项现在都是空的，
  是 GitHub 站内最直接的引流位，待用户确认文案
- 未在本地构建站点：本机无 Ruby / Jekyll

## 下一个会话要注意

- 部署后**必须**打开线上地址逐项核：Mermaid 是否出图（kramdown 把它渲染成
  `<pre><code class="language-mermaid">`，靠布局里那段脚本换容器后再渲染）、
  hreflang 是否成对、`./quickstart.md` 这类相对链接是否被 `jekyll-relative-links` 改写
- 站点根目录就是 `docs/guide`，所以 `index.md` 既是手册首页也是站点首页
- `robots.txt` 对本站无效：项目页在 `github.io` 的子路径下，爬虫只读域名根的那一份。
  收录靠 `sitemap.xml`（`jekyll-sitemap` 自动生成）+ 仓库内外的链接，别写一个假装有用的
  `robots.txt`
- `_config.yml`、`_layouts/`、`llms*.txt` 只在 `docs/guide/`，不在 `publish/`：
  它们是展示层，不是手册内容。镜像时只覆盖页面文件，别整目录对拷把它们删掉
