# 质量检查报告

> 检查时间：2026-09-20（返工版）
> 检查范围：Batch-1 至 Batch-4（全量重建）
> 运行模式：generate（重建）。上一轮 24 页产物已被整体取代
> 发布位置：`docs/guide/`（由 `docs/.doc-automation/publish/` 整目录镜像）

## 与上一轮的差异

| 维度 | 上一轮 | 本轮 |
|---|---|---|
| 页数 | 中英各 12 页 | 中英各 6 页 |
| 与 README 的关系 | 未盘点，54KB 手册与 23KB README 讲同一批事 | 取代：README 瘦身至 9.6KB，只留最短安装路径、国内网络、触发设计、安全模型 |
| 图 | 0 张 | 6 张（4 张 Mermaid + 1 张真实截图，截图被中英两页引用） |
| C 级运行时证据 | 0 条 | 5 条（4 份终端实录 + 1 张 `mp capture` 真实产物） |
| 英文页门禁 | 8 条告警被记为「脚本语言限制」后放行 | 与中文页同级阻断，全部通过 |
| 对外地址 | README 挂着指向 untracked 文件的 blob 链接（GitHub 上 404） | `docs/guide/` 已提交，门禁「对外入口」检查通过 |

## 检查结果总览

| 检查项 | 状态 | 阻断级别 | 详情 |
|--------|------|----------|------|
| 需求简报 | 通过 | — | 含新增的「既有文档与去重策略」章节 |
| 文档规划 | 通过 | — | 含新增的「规模预算」章节，6 页每语言 |
| 证据台账 | 通过 | — | 5 个本地证据文件全部存在 |
| capture 覆盖 | 通过 | — | 1 条截图类证据可重放，规格完整 |
| 链接有效性 | 通过 | — | 22 个相对引用全部可解析 |
| 发布结构 | 通过 | — | 12 个页面，必需页面齐全 |
| frontmatter | 通过 | — | 12 页字段齐全、日期合法、related 可解析 |
| 证据引用 | 通过 | — | 12 个编号全部存在于台账，无孤儿、无未引用 |
| 锚点覆盖 | 通过 | — | 10 个含规则性论断的页面均有 A/B 级锚点 |
| 术语一致性 | 通过 | — | 术语表 7 条，无误用 |
| MDX 兼容 | 通过 | — | 无加粗裸 URL |
| 文体契约 | 通过 | — | 无 emoji |
| Agent 可执行契约 | 通过 | — | 中英 12 页均无阻断项 |
| 视觉契约 | 通过 | — | 6 个流程页均含至少一张图 |
| 运行时证据 | 通过 | — | 台账含 5 条 C 级证据 |
| 双语镜像 | 通过 | — | en 与源语言各 6 页一一对应 |
| 对外入口 | 通过 | — | 2 个 README 的 4 个文件型链接全部已提交 |
| 漂移检测 | 通过 | — | 10 个页面 14 个锚点指纹全部一致 |
| 可执行性（人工） | 通过 | — | 手册中每条命令均在本机真跑过，正文「预期」与 `assets/transcripts/` 逐条对照 |
| 视觉一致性（人工） | 通过 | — | 唯一截图是 `mp capture` 的原始产物，未做标注，无风格冲突 |

## 阻断项

无。校验脚本退出码为 0。

## 降级记录

1. **`publish/ 建议包含 faq.md`（非阻断）。** 本次规模预算把 faq 列入扩展集并显式不写：
   它与 `troubleshooting.md` 的症状问答重叠，两页并存只会让读者不知道该查哪一页。
   需要时按「本次未覆盖」清单点单追加。
2. **2/12 个页面未声明 `source_anchors`（非阻断）。** 两个 `index.md` 的结论来自产品定位
   与用户需求，不来自某个源文件，按 `--drift` 的规则这类页面不必补锚点。
3. **Playwright MCP 本会话连接超时。** 不影响本次取证：截图走 `mp capture`
   （工具自带 Playwright），未使用 MCP。
4. **Image Annotator MCP 未使用。** 本次唯一截图不需要编号标注，直接用原图；
   因此也不存在「标注图缺图例」的检查对象。

## 本次未覆盖（供点单追加）

- `faq.md`：症状问答页
- `explanation/security.md`：安全边界独立成页（当前折在 index 的「两件必须先知道的事」
  与 README 的 Security 一节）
- `appendix/glossary.md`：术语表独立成页（当前术语首次出现即解释）
- `workflows/install-plugin.md`：插件安装独立成页（当前在命令速查末节链向插件 README）
- `mp interaction` 页面 HTML 的完整编写规范（在 `skill/SKILL.md` 与页面合同规格里）
- hooks 三个触发点的配置细节（在 `plugins/mobile-preview/README.md`）

## 校验脚本原始输出

```text

🔍 P4 发布门禁校验 — 目标：C:\Users\24408\IdeaProjects\mobile-preview\docs\.doc-automation

⚠️ [发布结构] publish/ 建议包含 faq.md
✅ [需求简报] context-brief.md 必需章节齐全
✅ [文档规划] doc-plan.md 必需章节齐全
✅ [证据台账] 5 个本地证据文件全部存在
✅ [capture 覆盖] 1 条截图类证据：可重放 1、显式不可重放 0、缺规格 0
✅ [链接有效性] publish/ 内 22 个相对引用全部可解析
✅ [发布结构] publish/ 含 12 个页面，必需页面齐全
✅ [frontmatter] 12 个页面的必需字段齐全、日期格式正确、related 可解析
✅ [证据引用] publish/ 引用的 12 个编号全部存在于台账
✅ [锚点覆盖] 10 个含规则性论断的页面均有 A/B 级锚点
✅ [术语一致性] 术语表 7 条，未登记任何旧名/别名，无需校验
✅ [MDX 兼容] 12 个页面无加粗裸 URL
✅ [文体契约] 12 个页面均未使用 emoji
✅ [Agent 可执行契约] 12 个页面无阻断项（流程页四段齐全、编号列表末步带预期）
✅ [视觉契约] 6 个流程页均含至少一张图
✅ [运行时证据] 台账含 5 条 C 级运行时证据
✅ [双语镜像] en 与源语言页面一一对应（各 6 页）
✅ [对外入口] 2 个 README 中 4 个文件型链接全部已提交

✅ 无阻断项，允许发布（1 个 ⚠️ 项须记录到 quality-report.md 降级记录）。
```
